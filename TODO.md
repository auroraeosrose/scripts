Stuff yet to finish
=======

1. Add the windows vm/build setup scripts
2. Automate the ubuntu ick removal process at some point
3. Automate the ubuntu install process (including bison build) at some point
4. Add gcov, code analyze, and leak detector versions of the phpize script
5. Add dep build scripts for things like gtkforphp


